# Optional Flutter Web Client (Scaffold)

This folder is a placeholder for an optional Flutter Web client for `trackMoney`.

## Suggested next steps
1. Run `flutter create .` in this folder.
2. Add API integration for the FastAPI backend.
3. Recreate auth, dashboard, and expense CRUD screens.

The main required frontend for CS50 compliance is in `frontend/streamlit_app.py`.
